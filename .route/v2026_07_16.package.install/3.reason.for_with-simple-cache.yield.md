# reason: with-simple-cache@0.17.0

## package
- name: with-simple-cache
- version: 0.17.0
- type: prep

## reason
types-only: proves the exported cache satisfies the real WithCacheConditionals<SimpleCacheAsync<string>> contract (conformance test). import type is erased at compile, so no runtime edge despite with-simple-cache depending on this package. declared peer (published contract, consumers supply it) + dev (local typecheck).
