# 2.detect.hazards — result

## summary

3 potential hazards detected, 1 likely to cause issues.

---

## hazard 1: cicd — aws role variable rename

**files**: `.github/workflows/test.yml`, `.github/workflows/publish.yml`

**change**:
```diff
- creds-aws-role-arn: ${{ vars.CREDS_CICD_AWS_PREP_OIDC_ROLE_ARN }}
+ creds-aws-role-arn: ${{ vars.CREDS_CICD_AWS_DEV_OIDC_ROLE_ARN }}
```

**risk**: high — if `CREDS_CICD_AWS_DEV_OIDC_ROLE_ARN` variable is not set in GitHub repo settings, CICD will fail on any step that requires AWS auth.

**verify**: run tests in CI after push, check for AWS auth errors.

**note**: this repo may not use AWS integration tests, in which case this is a no-op. need to verify.

---

## hazard 2: lint — dpdm exclude list cleared

**file**: `.dpdmrc.yaml`

**change**:
```diff
- exclude:
-   - bottleneck
-   - '@aws-crypto'
+ exclude: []
```

**risk**: medium — if any cycles exist in the codebase or dependencies, `npm run test:lint:cycles` will now detect them and fail.

**verify**:
```sh
rhx git.repo.test --what lint --mode apply
```

**note**: this is likely intentional. previous commit replaced `bottleneck` with `with-bottleneck` (which has no cycles). `@aws-crypto` was possibly a false positive or no longer used.

---

## hazard 3: test — jest cache exclusion

**files**: `jest.unit.config.ts`, `jest.integration.config.ts`, `jest.acceptance.config.ts`

**change**: added `!**/.agent/.cache/**` to `testMatch` exclusions

**risk**: low — this just excludes cache files from test match patterns. should not affect test behavior.

**verify**: run tests to confirm no regressions.

---

## safe changes (no hazards)

- `biome.jsonc`: added `.agent/.cache` exclusion — safe
- `tsconfig.json`: added `.agent/.cache` exclusion — safe
- `provision/github.repo/resources.ts`: type safety refactor — safe
- `package.json`: version bumps for rhachet packages — safe
- `with-bottleneck`: 0.1.0 → 0.1.1 — minor version bump, likely safe

---

## next steps

1. run lint to verify hazard 2 (dpdm cycles)
2. run tests to verify overall health
3. if failures occur, repair in next stones
