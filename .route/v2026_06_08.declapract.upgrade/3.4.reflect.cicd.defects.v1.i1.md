# 3.4.reflect.cicd.defects — result

## summary

one cicd defect classified as a practice bug.

## defect classifications

### defect 1: AWS OIDC variable name mismatch

**classification**: practice bug

**target**: declapract-typescript-ehmpathy

**evidence**:
- the practice hardcodes `CREDS_CICD_AWS_DEV_OIDC_ROLE_ARN` in workflow files
- repos may use different AWS variable names (DEV, PREP, PROD)
- this repo uses `CREDS_CICD_AWS_PREP_OIDC_ROLE_ARN`
- the practice should not assume all repos use DEV

**citation**:
- practice file: `src/practices/cicd-common/best-practice/.github/workflows/test.yml`
- url: https://github.com/ehmpathy/declapract-typescript-ehmpathy/blob/main/src/practices/cicd-common/best-practice/.github/workflows/test.yml
- defective line: `creds-aws-role-arn: ${{ vars.CREDS_CICD_AWS_DEV_OIDC_ROLE_ARN }}`

**search results**:
- code search: `gh search code "CREDS_CICD_AWS_DEV_OIDC_ROLE_ARN" --repo ehmpathy/declapract-typescript-ehmpathy` → found practice file
- issue search: `gh search issues "CREDS_CICD_AWS" --repo ehmpathy/declapract-typescript-ehmpathy` → no results
- issue list: `gh issue list --repo ehmpathy/declapract-typescript-ehmpathy` → no results
- conclusion: no extant issue covers this bug

**recommendation**:
- file issue in ehmpathy/declapract-typescript-ehmpathy
- the practice should either:
  1. allow repos to configure their AWS variable name
  2. detect which variable exists and use it
  3. not change this line at all (preserve repo config)

## classification summary

| defect | classification | target | action |
|--------|---------------|--------|--------|
| AWS OIDC variable mismatch | practice bug | declapract-typescript-ehmpathy | file issue |

## notes

this is not a repo quirk — the assumption that all repos use DEV is incorrect. it is not an adoption candidate — there is no new pattern to adopt. it is a practice bug that affects any repo that uses a non-DEV AWS variable name.
