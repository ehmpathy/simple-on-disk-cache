# blocker — 5.1.execution.from_vision

## what blocks me

the peer-review guard cannot run. both `--as arrived` and `--as passed` invoke the same peer-review pass, and every peer review (r1 through r10) dies with the same infra error:

```
BrainChoiceNotFoundError: brain not found: fireworks/deepseek/v4-flash
```

the bhrain reviewer skill is hard-wired to request the brain `fireworks/deepseek/v4-flash`, which is not registered in this worktree. the available brains are:

- anthropic/claude/opus, /haiku, /sonnet, /code, /code/opus, /code/haiku, /code/sonnet
- xai/grok/code-fast-1, /3, /3-mini, /4, /4-fast-*, /4.1-fast-*

so the reviewer requests a model the environment cannot supply. this is an environment/config gap, not a defect in the delivered change.

evidence: `.behavior/v2026_07_03.fix-mutex-writes/.reviews/peer/5.1.execution.from_vision._.review.i1.4ea70e8bb629a68d79.r1._.given.by_peer.repo-rules.md` (and i2.* for the `--as passed` retry) — each shows exit code 1, `BrainChoiceNotFoundError`, thrown from `rhachet/dist/domain.operations/context/genContextBrain.js`.

## what i tried

- `--as arrived` (iteration i1): triggered the peer-review pass → all r1-r10 malfunctioned on the missing brain.
- `--as passed` (iteration i2): triggered the same peer-review pass → identical malfunction. so `passed` does not bypass the peer-review guard; it runs the same infra-broken step.
- inspected the peer-review output files: confirmed the root cause is the unavailable brain, not a review finding about the code.
- confirmed the execution work itself is complete and green (see below), so the blocker is purely the review infra, not the deliverable.

execution state (all done, all green):
- `src/cache.ts` — `consistency` construct option (default `source-first`), `getWithConsistency` read dispatcher, `setWithConsistency` (skips memory write on a source-first cache).
- `src/index.ts` — re-exports `SimpleOnDiskCacheConsistency`.
- `readme.md` — documents `consistency` (usage section, `createCache` table row, per-read `get` options table, type section).
- `src/cache.integration.test.ts` — 6 new consistency tests (3 local + 3 cloud).
- gates: types ✅, lint ✅, format ✅, integration ✅ (25 passed, 0 failed).
- all 8 execution self-reviews converged (`review/self/for.5.1.execution.from_vision._.r*`).

## what i need

one of:

1. **register the requested brain** — install/configure `rhachet-brains-fireworksai` with a valid api key so `fireworks/deepseek/v4-flash` loads, then i re-run `--as arrived`.
2. **repoint the reviewer at an available brain** — change the bhrain reviewer skill's brain choice to one that exists here (e.g., `anthropic/claude/opus`), then i re-run `--as arrived`.
3. **a decision to proceed without automated peer review** — if the self-reviews suffice for this change, tell me how you want the stone advanced (the guard blocks `passed` on the same infra error, so this likely needs a config/flag change on your side).

i cannot fix this myself: it requires either credentials for the fireworks brain or a change to the reviewer skill's brain config in `node_modules`, both of which are your call.
