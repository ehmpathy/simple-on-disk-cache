# execution: remote adapter dependency injection

## progress

- [x] add `SimpleOnDiskCacheCloudAdapter` type to `cache.ts`
- [x] update `DirectoryToPersistTo` from `{ mounted } | { s3 }` to `{ local } | { cloud }`
- [x] update `saveToDisk` and `readFromDisk` to use adapter
- [x] update `src/index.ts` to export new type
- [x] remove `@aws-sdk/client-s3` from dependencies
- [x] remove `src/utils/s3/` directory
- [x] update `.dpdmrc.yaml` — kept `@aws-crypto` for dev deps (sdk-aws-s3 brings it)
- [x] update tests to use `sdk-aws-s3` package
- [x] types pass
- [x] format pass
- [x] lint pass
- [x] integration tests — passed (18 tests, 51s)

## notes

- `sdk-aws-s3` is live and available
- adapter supports both `get.one` (namespace) and `get` (direct function) styles
- returns `null` for not-found (not `undefined`)
- `@aws-crypto` exclusion kept in `.dpdmrc.yaml` because `sdk-aws-s3` (dev dep) brings it transitively
- published package has no aws-sdk deps — users get 2MB lighter install
