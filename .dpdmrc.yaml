# dpdm exclude patterns
# each entry is joined with | to form a single regex for --exclude
#
# exclude specific third-party packages with internal cycles
# only exclude packages known to have internal cycles, not all node_modules
exclude:
  - bottleneck
  - '@aws-crypto'
